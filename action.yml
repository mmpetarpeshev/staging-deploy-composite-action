name: 'Staging Deploy'
description: 'Staging Deploy'
inputs:
  pr-number:
    description: 'PR Number'
    required: true
runs:
  using: "composite"
  steps:
    - run:  ${{ inputs.pr-number }}.
      shell: bash
    - run: ${{ github.action_path }}/staging-deploy.sh ${{ inputs.pr-number }}.
      shell: bash
